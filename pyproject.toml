[project]
name = "roboshop-load-tester"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"
